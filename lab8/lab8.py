"""by Roman Polishchenko2 course, comp mathTaras Shevchenko National University of Kyivemail: roma.vinn@gmail.com"""# import numpy as npimport copydef compare_vectors(a: list, b: list) -> bool:    """    Перевірка чи а > b (по всім координатам)    :param a: список координат    :param b: список координат    :return: True, якщо а > b, False - інакше    """    assert len(a) == len(b)    flag = True    for i in range(len(a)):        if a[i] < b[i]:            flag = False            break    return flagdef add_step(i, left, a, step, b, add=False):    """ #TODO написати пояснення    :param i:    :param left:    :param a:    :param step:    :param b:    :param add:    :return:    """    if add:        a[i] += step    if a[i] > b[i]:        if i != 0:            a[i] = left[i]            add_step(i-1, left, a, step, b)        else:            return Falsedef measure(func: callable, borders, n=4):    """ #TODO написати пояснення    :param func:    :param borders:    :param n:    :return:    """    left, right = borders    m = len(left)    step = 1/2**n    curr = copy.deepcopy(left)    while True:        if not add_step(len(curr), left, curr, step, right, add=True):            breakif __name__ == '__main__':    pass